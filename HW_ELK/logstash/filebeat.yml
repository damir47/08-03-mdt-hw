filebeat.inputs:
- type: filestream
  id: nginx_access
  enabled: true
  paths:
    - /var/log/nginx/access.log
  fields:
    service: nginx
    environment: production

output:
  logstash:
    enabled: true
    hosts: ["logstash:5044"]
    ssl:
      enabled: false
